.atl
.env
.temp
docker-reference
moodle-code
plugins-reference

# mkcert certs and keys (keep the directory via .gitkeep, ignore the contents)
docker/caddy/certs/*
!docker/caddy/certs/.gitkeep
data
debug-errors
